{
  "format_version": "1.10.0",
  "animation_controllers": {
    "controller.animation.game_start_curtain": {
      "states": {
          "default": {
              "transitions": [
                { "game_start_curtain_close": "query.is_alive && query.mark_variant == 554" }
              ],
              "blend_transition": 0.3
          },
          "game_start_curtain_close": {
            "animations": [
              "game_start_curtain_close_anim"
            ],
            "transitions": [
              { "game_start_curtain_open": "query.mark_variant != 554 || query.is_item_name_any('slot.weapon.mainhand', 0, 'game:camera')" },
              { "default": "query.mark_variant == 0" }
            ]
          },
          "game_start_curtain_open": {
            "animations": [
              "game_start_curtain_open_anim"
            ],
            "transitions": [
              { "default": "query.is_alive && query.all_animations_finished && (query.mark_variant == 0 || (query.mark_variant >= 554 && query.mark_variant <= 562))" }
            ]
          }
      }
  }
  }
}
