{
  "format_version": "1.10.0",
  "animation_controllers": {
    "controller.animation.camera_line": {
      "states": {
        "default": {
          "transitions": [
            { "camera_line": "q.is_alive && query.mark_variant >= 555" }
          ]
        },
        "camera_line": {
          "animations": [
            "camera_line_anim"
          ],
          "transitions": [
            { "default": "query.mark_variant < 555 && query.mark_variant != 20" }
          ]
        }
      }
    }
  }
}
